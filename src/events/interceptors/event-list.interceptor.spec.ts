import { EventListInterceptor } from './event-list.interceptor';

describe('EventListInterceptor', () => {
  it('should be defined', () => {
    expect(new EventListInterceptor()).toBeDefined();
  });
});
