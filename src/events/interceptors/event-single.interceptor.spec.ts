import { EventSingleInterceptor } from './event-single.interceptor';

describe('EventSingleInterceptor', () => {
  it('should be defined', () => {
    expect(new EventSingleInterceptor()).toBeDefined();
  });
});
